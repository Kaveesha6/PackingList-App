specVersion: "v1"
serviceId: "packinglist-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "packinglist-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
